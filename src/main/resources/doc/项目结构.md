src/main/
├── java/com/yourname/weather/
│   ├── entity/          # 存放 WeatherForecast, WeatherResponse
│   ├── mapper/          # 存放 WeatherMapper.java (接口)
│   ├── service/         # 存放业务逻辑代码
│   ├── utils/           # 存放 MyBatisUtils, HttpUtils 等
│   └── App.java         # 启动类
│
└── resources/           # 【配置文件在这里】
├── mapper/
│   └── WeatherMapper.xml   # 具体的 SQL 语句在这里
├── db.properties           # 存放数据库账号密码 (可选，推荐)
└── mybatis-config.xml      # MyBatis 的核心开关和设置